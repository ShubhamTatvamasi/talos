cluster:
  allowSchedulingOnControlPlanes: true
machine:
  kubelet:
    extraMounts:
      - destination: /var/openebs
        type: bind
        source: /var/openebs
        options:
          - bind
          - rshared
          - rw
